* Объектное хранилище (Object Storage) — это метод хранения данных, 
при котором информация сохраняется в виде объектов (файлов) вместе с метаданными и уникальным идентификатором (ID). 
В отличие от блочных (SAN) и файловых (NAS) хранилищ, объектное хранилище не использует иерархическую структуру папок, 
а обращается к данным через API (например, RESTful HTTP-запросы).

* Архитектура
Ноды хранения (Storage Nodes) — серверы, хранящие объекты.
Метаданные сервера (Metadata Server) — управляет индексами и ID объектов.
API-шлюз (Gateway) — обрабатывает запросы клиентов (HTTP/S3).
Балансировщик нагрузки — распределяет запросы между нодами.

* Алгоритмы хранения
Репликация (копирование объектов на несколько узлов).
Erasure Coding (разделение данных на фрагменты с избыточностью для восстановления при сбоях).

Erasure coding Стирающий код — в теории кодирования помехоустойчивый код, 
способный восстановить целые пакеты данных в случае их потери.

Erasure Coding (кодирование с удалением)
Файл разбивается на N частей, из которых вычисляются K дополнительных частей (контрольные суммы).

В Erasure Coding контрольная сумма (или "parity block") — это дополнительный блок данных, 
который вычисляется математически из исходных блоков. 
Он позволяет восстановить потерянные данные, даже если часть серверов упала.

Чем отличается от обычной "контрольной суммы" (CRC, MD5)?
Обычная контрольная сумма (CRC, хеш) — просто проверяет целостность данных (например, чтобы обнаружить ошибки).
Контрольная сумма в Erasure Coding — не только проверяет, но и позволяет восстановить утерянные данные!

контрольная сумма в Erasure Coding (EC) — это не сжатая версия одного конкретного блока, а результат математической операции над несколькими блоками сразу.

В кодировании Reed-Solomon (популярный метод EC) контрольные суммы вычисляются так:

Дано:
D1, D2 — исходные блоки данных.
P1, P2 — контрольные суммы.

Формулы:
P1 = D1 + D2  
P2 = D1 + 2*D2  
(Здесь + и * — операции в конечном поле, но для простоты считаем их обычными сложением/умножением.)

 Пример с числами
Пусть:
D1 = 10
D2 = 20

Тогда:
P1 = 10 + 20 = 30
P2 = 10 + 2*20 = 50

Теперь распределим блоки по серверам:
Сервер 1: D1 = 10
Сервер 2: D2 = 20
Сервер 3: P1 = 30
Сервер 4: P2 = 50

Как восстановить данные из D2 + P2?
Допустим, у нас есть только:
D2 = 20
P2 = 50

Шаг 1: Записываем уравнения
Из формул выше:
P2 = D1 + 2*D2 → 50 = D1 + 2*20
D2 = 20 (уже знаем)

Шаг 2: Решаем уравнение
Из первого уравнения:
D1 + 40 = 50  
D1 = 50 - 40  
D1 = 10  
Всё! Мы восстановили D1.

Теперь у нас есть:
D1 = 10 (восстановленный)
D2 = 20 (изначально был)
Исходный файл = D1 + D2 = [10, 20].

Почему это работает?
Контрольные суммы (P1, P2) содержат "перемешанную" информацию обо всех блоках, а не об одном.
P2 зависит и от D1, и от D2, поэтому даже если есть только D2 + P2, можно вытащить D1.

Аналогия:
Представь, что:
D1 и D2 — два числа,
P1 и P2 — подсказки, которые связывают их.
Если знаешь одно число (D2) и одну подсказку (P2), можно найти второе число (D1).

А если бы P2 зависел только от D2?
Тогда восстановить D1 было бы невозможно!
Но в EC контрольные суммы смешивают данные всех блоков, поэтому:
P1 = D1 + D2
P2 = D1 + 2*D2
Это гарантирует, что любые 2 блока (из 4) позволяют восстановить исходные данные.

Вывод
Контрольная сумма — не сжатый блок, а результат операции над несколькими блоками.
Восстановление возможно, потому что P2 содержит информацию и о D1, и о D2.
Чем больше контрольных сумм (K), тем больше комбинаций для восстановления.

Контрольные суммы в EC — это не просто копии блоков, а линейные комбинации всех исходных данных.
Каждая контрольная сумма "знает" что-то обо всех блоках.
Если уравнений (контрольных сумм) достаточно, можно решить систему и найти D1, D2, ....

🔹 Когда восстановление невозможно?
Если контрольных сумм меньше, чем исходных блоков (K < N).
Пример (схема 4+2):
Исходные данные: D1, D2, D3, D4
Контрольные суммы: P1, P2
Если есть только P1 и P2, уравнений меньше, чем неизвестных (4 переменных при 2 уравнениях).
Решение: Невозможно однозначно восстановить данные.

Можно восстановить данные только из контрольных сумм, если их достаточно (K >= N).
Чем больше K, тем выше отказоустойчивость (можно терять больше блоков).
Контрольные суммы — это уравнения, а исходные данные — переменные.