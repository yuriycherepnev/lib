Протоколы (прикладного уровня):
HTTP (Hyper Text Transfer Protocol) - гипертекст
FTP (File Transfer Protocol) - файлы
SMTP (Simple Mail Transfer Protocol) - почта
TELNET - протокол удаленного доступа
SSH - сетевой протокол прикладного уровня, позволяющий производить удалённое управление операционной системой (шифрует весь трафик)
WebSocket - протокол связи поверх TCP-соединения, предназначенный для обмена сообщениями между браузером и веб-сервером, используя постоянное соединение.

Протоколы транспортного уровня:
TCP (Transmission Control Protocol) — гарантирует передачу всех данных без потерь. Полезен при отправке текстовых файлов. (предварительная установкой соединения)
UDP (User Datagram Protocol) — не гарантирует передачу данных без потерь, но обеспечивает хорошую скорость. Полезен при просмотре видео или прослушивании музыки в интернете.

Методы запроса:
GET - для получения данных
HEAD -  обычно применяется для извлечения метаданных, проверки наличия ресурса (валидация URL).
POST - применяется для передачи пользовательских данных заданному ресурсу.
PUT - применяется для загрузки содержимого запроса на указанный в запросе URI.
DELETE - удаляет указанный ресурс.

Структура HTTP:
Стартовая строка запроса - строка которая, содержит в себе метод, адрес, версию протокола
метод запроса, путь к документу, версия протокола:

GET /wiki/HTTP HTTP/1.0
Host: ru.wikipedia.org

Стартовая строка ответа:
версия, код состояние, пояснение
HTTP/1.0 200 OK

заголовки: это строки в HTTP-сообщении, содержащие разделённую двоеточием пару параметр-значение. 
Server: Apache/2.2.11 (Win32) PHP/5.3.0
Last-Modified: Sat, 16 Jan 2010 21:16:42 GMT
Content-Type: text/plain; charset=windows-1251
Content-Language: ru

Тело запроса:

POST /login HTTP/1.1
Host: example.com
Content-Type: application/x-www-form-urlencoded; charset=utf-8
Content-Length: 28

login=user&password=123%3D45

HTTP и HTTPS:
есть сертификат от центра сертификации
тело шифруется
поисковая выдача выше
порт 80 и 443

Модель OSI (Open System Interconnection) - это модель взаимодействия открытых систем, которая описывает
как происходит обмен данными в локальных и глобальных сетях.

Модель TCP/IP — это стек протоколов, которые задают правила передачи данных по Сети. 
Так как бал здесь правят протоколы TCP и IP, в честь них и назвали всю модель.

TCP (Transmission Control Protocol) отвечает за обмен данными. Он управляет их отправкой и следит за тем, 
чтобы они дошли до получателя в целости. У TCP есть свои гарантии, что всё пройдёт успешно, — о них чуть позже.

IP (Internet Protocol) отвечает за адресацию. Его задача — связывать друг с другом устройства и нарезать 
данные на пакеты для удобной отправки. Чтобы протокол мог быстро найти дорогу от одного компьютера к другому, 
придумали IP-адреса — уникальные идентификаторы, которые есть у каждого устройства в Сети.

API (application programming interface) - это мост, который позволяет разным программам обмениваться информацией и работать вместе.

Путь запроса:

Повторим ещё раз весь процесс:

- Вы нажимаете на ссылку или вводите адрес сайта в строке браузера.
- Браузер создаёт HTTP-запрос к серверу, чтобы тот отправил вам нужную страницу.
- Протокол IP с помощью системы DNS находит сервер, где лежит страница, и разбивает её на пакеты.
- Далее в игру вступает TCP — он устанавливает надёжное соединение между компьютером и сервером и следит, чтобы пакеты не потерялись по дороге.
- Браузер склеивает пакеты воедино и рендерит страницу.

Этот алгоритм справедлив для статичных файлов — то есть таких, которые вы загружаете один раз и пользуетесь. Но если вы, например, хотите посмотреть видео, понадобится другой протокол — UDP. Он не гарантирует целостности данных и может потерять пакеты по пути, но это компенсируется скоростью передачи. Именно поэтому, например, вы видите пиксельные изображения, когда смотрите видео при плохом интернет-соединении.

Индекс - объект базы данных, создаваемый с целью повышения производительности поиска данных. 

Типы Индексов:

Кластеризованный индекс хранит реальные строки данных в листьях индекса
Некластеризованный индекс содержит только те столбцы (ключевые), по которым определен данный индекс

Составной индекс - более 1 столбца
Уникальный индекс

Первичный ключ - Кластеризованный Уникальный индекс.
