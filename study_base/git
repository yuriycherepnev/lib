git merge origin/bonus_system

мерджит локальную ветку на которой мы находимся с удаленной веткой bonus_system
в репозитории origin

--------------------------------------------------------------------
git push origin bonus_system

пушит локальную ветку bonus_system в удаленный репозитории origin
если такая ветка есть - обновляет ее, если нет - создает